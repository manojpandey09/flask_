# Flask Application (Backend Only)

This is a simple backend application built using **Flask**.  
The project focuses on Python and Flask routing without using HTML or CSS.

## 🚀 Features
- REST-style routes
- JSON responses
- Lightweight and fast backend
- Easy to extend with database or authentication

## 🛠️ Technologies Used
- Python 3
- Flask

## 📂 Project Structure
